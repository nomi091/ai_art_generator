import 'package:flutter/material.dart';
class JustScreen extends StatelessWidget {
  const JustScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(

      appBar: AppBar(
        title: Text('Justscreen'),
      ),
    );
  }
}
